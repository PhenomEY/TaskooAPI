monolog:
    handlers:
        main:
            type: stream
            action_level: error
            handler: nested
            channels: ["!event"]
        nested:
            type: stream
            path: "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug
